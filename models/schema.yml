version: 2

models:
  - name: hub_customer_dv
    description: One record per customer
    columns:
      - name: customer_hk
        description: Primary key
        data_tests:
          - unique
          - not_null
  
  - name: sat_customer_dv
    description: One record per customer
    columns:
      - name: customer_hk
        description: Primary key
        data_tests:
          - unique
          - not_null