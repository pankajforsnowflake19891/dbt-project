version: 2
models:
  - name: hub_customerforhub
    description: '{{doc("hub_customerforhub ")}}'
    config:
      tags: ['Refresh=Daily']
      meta:
        data classification: 'Internal'
        natural key: 'customer_id'
        primary keys: 'customer_HK'
        refresh interval: 'Refresh=Daily'
    columns:
      - name: customer_HK
        description: '{{doc("customer_HK_in hub_customerforhub")}}'
        tests:
          - not_null
          - unique
      - name: customer_id
        description: '{{doc("customer_id_in_hub_customerforhub")}}'
      - name: load_dts
        description: '{{doc("load_dts_in_hub_customerforhub")}}'
      - name: source
        description: '{{doc("source_in_hub_customerforhub")}}'
     
